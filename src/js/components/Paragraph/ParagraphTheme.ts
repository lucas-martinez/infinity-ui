import ExtendType from '../Theme/ExtendType';

export default interface ParagraphTheme {
  extend: ExtendType;
  small: {
    size: string;
    height: string;
    maxWidth: string;
  };
  medium: {
    size: string;
    height: string;
    maxWidth: string;
  };
  large: {
    size: string;
    height: string;
    maxWidth: string;
  };
  xlarge: {
    size: string;
    height: string;
    maxWidth: string;
  };
  xxlarge: {
    size: string;
    height: string;
    maxWidth: string;
  };
}
