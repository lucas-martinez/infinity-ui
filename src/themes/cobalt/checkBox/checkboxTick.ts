import spec from '../quantum-spec';

export const checkboxTick = {
  animationChecked: spec.global.checkboxTick.animationChecked,
  color: {
    dark: spec.global.checkboxTick.colorDark,
    light: spec.global.checkboxTick.color,
  },
};