import spec from '../../quantum-spec';

const container = {
  height: spec.global.buttonPaginationContainer.height,
  margin: spec.global.buttonPaginationContainer.margin
};

export default container;
